=== USER PROMPT ===
BASENAME={{ BASENAME }}
Attached (as inlines or physical files):
* MusicXML file "{{ BASENAME }}.xml"
* Analysis JSON file "{{ BASENAME }}_analysis.json"

Use the "{{ BASENAME }}_analysis.json" as descriptive facts only (keys, harmonies, cadence points, observed textures, ranges). Do not treat it as a plan; follow the SYSTEM for textures and constraints.
